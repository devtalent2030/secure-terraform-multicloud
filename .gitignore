##############################################
# Terraform Build Artifacts & State Files
##############################################

# Local .terraform directories (hold provider binaries & cached plugins)
**/.terraform/*

# Terraform state files (contain resource metadata & sometimes secrets!)
*.tfstate
*.tfstate.*

# Crash log files (created if Terraform crashes mid-run)
crash.log
crash.*.log

# Lock file that records exact provider versions
# (safe to commit in collaborative repos, but here we ignore for lab simplicity)
.terraform.lock.hcl

##############################################
# Sensitive Input Variables
##############################################

# Generic .tfvars files often contain sensitive data (keys, passwords, tokens).
# These should NEVER be checked in — each engineer keeps their own copy.
terraform.tfvars
*.tfvars
*.tfvars.json
*.tfvars.local

##############################################
# Override & Local Experimentation Files
##############################################

# Override files are for local dev tweaks and should not be shared
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform creates a transient lock info file during apply
.terraform.tfstate.lock.info

# If you run `terraform plan -out=tfplan`, ignore the binary tfplan files
*tfplan*

##############################################
# Terraform CLI Config (per-user, not repo)
##############################################
.terraformrc
terraform.rc

##############################################
# Cloud Provider Credentials
##############################################

# GCP Service Account JSON (never commit to GitHub!)
gcp-terraform-sa.json

# Private keys, certs
*.pem
*.key

##############################################
# OS & Editor Junk
##############################################

# macOS Finder files
.DS_Store

# IDE/editor configs (safe to ignore unless team standardizes on them)
.idea/
.vscode/
